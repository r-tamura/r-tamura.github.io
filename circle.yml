machine:
  node:
    version: 8.9.1
dependencies:
  override:
    - yarn install
deployment:
  production:
    branch: source
    commands:
      - git config --global user.name "CircleCI"
      - git config --global user.email "rtamura30@gmail.com"
      - npm run clean
      - npm run build
      - npm run deploy
general:
  branches:
    ignore:
      - master
notify:
  webhooks:
    # A list of hook hashes, containing the URL field
    - url: https://hooks.slack.com/services/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
experimental:
  notify:
    branches:
      ignore:
        - master
